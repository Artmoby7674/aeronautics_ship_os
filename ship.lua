-- Identifies which ship this computer is installed on.
-- Ship packs / future players drop this file next to startup.
-- profile = config template name under config/<profile>.lua (also used as save-slot template).

return {
    id = "Atlas",
    name = "Atlas",
    profile = "atlas",
}
